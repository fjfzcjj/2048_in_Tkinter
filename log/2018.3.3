GameWindow and GameMatrix are separated.
Grid is better than Frame to organize elements
use .......grid(row = ..., column = ..., sticky = ...., columnspan =... ) to place object

sticky = NWSE   to left align/ right align.

columnspan  to define how high the element is

easy way to create list
    buttons = [tk.Buttons(master) for i in range(16)]

use format:
options = {a:.....,
           b:.....
}

to organize keyword arguments

to unpack tuple:
y = (x,y)
a,b = y